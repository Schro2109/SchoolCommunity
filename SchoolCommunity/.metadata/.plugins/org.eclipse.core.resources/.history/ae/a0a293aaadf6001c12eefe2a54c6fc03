DROP TABLE ACCOUNT;
CREATE TABLE ACCOUNT(
	ID VARCHAR2(20) PRIMARY KEY NOT NULL,
	PASSWORD VARCHAR2(20) NOT NULL,
	NAME VARCHAR2(30) NOT NULL
);
INSERT INTO ACCOUNT VALUES('admin','1234','관리자');

DROP TABLE POST_NEWS;
CREATE TABLE POST_NEWS(
	N_PCODE NUMBER(6) PRIMARY KEY NOT NULL,
	WRITER VARCHAR2(20) NOT NULL,
	TITLE VARCHAR2(100) NOT NULL,
	CONTENTS VARCHAR2(1000),
	FOREIGN KEY (WRITER) REFERENCES ACCOUNT (ID)
);
DROP TABLE POST_FREE;
CREATE TABLE POST_FREE(
	F_PCODE NUMBER(6) PRIMARY KEY NOT NULL,
	WRITER VARCHAR2(20) NOT NULL,
	TITLE VARCHAR2(100) NOT NULL,
	CONTENTS VARCHAR2(1000),
	FOREIGN KEY (WRITER) REFERENCES ACCOUNT (ID)
);
DROP TABLE POST_QNA;
CREATE TABLE POST_QNA(
	Q_PCODE NUMBER(6) PRIMARY KEY NOT NULL,
	WRITER VARCHAR2(20) NOT NULL,
	TITLE VARCHAR2(100) NOT NULL,
	CONTENTS VARCHAR2(1000),
	FOREIGN KEY (WRITER) REFERENCES ACCOUNT (ID)
);
DROP TABLE COMMENT;
CREATE TABLE COMMENT(
	CMCODE
);
DROP TABLE REPLY;
CREATE TABLE REPLY(
	RCODE NUMBER
);